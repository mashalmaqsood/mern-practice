function sum(a,b){
    return a + b;
}

console.log(sum(10,"10"))

// If either operand is a string, JavaScript converts the other to a string and performs string concatenation.
// 10 + "10" → JavaScript coerces the number 10 to a string "10"